# How to use the server and client

As long as you have Python installed you shouldn't have to install any dependencies 

So just run the code following the instructions below



In one terminal enter the command


```
python server.py 8008
```

In another terminal run the command

```
python client.py 127.0.0.1 8008
```
